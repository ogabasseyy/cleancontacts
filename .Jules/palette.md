## 2024-06-22 - Add tactile feedback to CTAs
**Learning:** Users lack confidence when primary Call-to-Action buttons don't provide immediate visual feedback upon click (like a subtle scaling effect). This makes the interface feel less responsive.
**Action:** Apply `active:scale-95 transition-all duration-200` to all primary CTA buttons to ensure a consistent, satisfying tactile response.
